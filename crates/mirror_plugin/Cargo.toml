[package]
name = "mirror_plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
 
[dependencies]
image_processor_plugin = { path = "../image_processor_plugin" }
abi_stable.workspace = true
image.workspace = true
